// Function Implementation
// tail() function that takes in an array as an arg
// and returns the same array with its first element removed

const tail = function(array) {
  return array.slice(1);
};

module.exports = tail;
